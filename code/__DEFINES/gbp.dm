//* defines for the gbp (good boy points) module, so called that because point economy is so silly *//

#define GBP_TYPE_SURVEY "survey"

GLOBAL_REAL_LIST(gbp_names) = list(
	GBP_TYPE_SURVEY = "Survey",
)

