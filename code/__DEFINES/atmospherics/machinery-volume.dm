//* standard machinery volumes (in liters) *//

/// small, so they're usually masked over by the volume of a component.
#define ATMOS_DEFAULT_VOLUME_PIPE_PER_SIDE 12.5
/// larger so heat exchanger pipes draw more, well, heat.
#define ATMOS_DEFAULT_VOLUME_EXCHANGER_PIPE_PER_SIDE 50

/// relatively large compared to pipes
///
/// * this is per node / side by default
#define ATMOS_DEFAULT_VOLUME_COMPONENT 200
