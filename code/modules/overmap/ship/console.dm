#warn circuit

/obj/machinery/computer/ship

#warn impl all

/**
 * return a list of control columns linked to us
 */
/obj/machinery/computer/ship/proc/column_query()
	#warn impl

/**
 * check if we're still connected to a column
 */
/obj/machinery/computer/ship/proc/column_reachable(obj/machinery/ship_control/column)
	#warn impl

