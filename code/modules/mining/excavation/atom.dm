/**
 * allow drilling
 * if called with a user, the user tried to start drilling with a physical tool
 * feedback/aftereffects are allowed, this doesn't have to be a pure proc
 */
/atom/proc/CanExcavate(obj/item/pickaxe/P, mob/user)
