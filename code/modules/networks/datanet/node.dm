/**
 * the base level of connectivity
 * handles all network operations
 * unique ids must be assigned to these.
 */
/datum/data_node

/datum/data_node/proc/receive_packet(datum/data_packet/packet, flags)
	return
