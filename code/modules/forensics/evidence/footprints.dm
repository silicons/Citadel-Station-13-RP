/**
 * Footprints module
 *
 * Tracking through certain objects produces a trail of footprints
 *
 * This is a piece of evidence visually identified/is scannerless,
 * but is even less  reliable than fibers.
 *
 * Repeatedly stepping through the same footprints will muddle them.
 */
/atom
	/// Footprints
	var/list/footprints
